server:
  port: 8080

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/PhoneInventoryManager"
    username: root
    password: m/ng2k56
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      format-sql: true
    show-sql: true
  servlet:
    multipart:
      max-file-size: 500KB
      max-request-size: 500KB
